import { SecondConversionPipe } from './second-conversion.pipe';

describe('SecondConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
