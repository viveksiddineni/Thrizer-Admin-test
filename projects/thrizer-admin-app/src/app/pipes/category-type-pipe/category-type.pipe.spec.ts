import { CategoryTypePipe } from './category-type.pipe';

describe('CategoryTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryTypePipe();
    expect(pipe).toBeTruthy();
  });
});
