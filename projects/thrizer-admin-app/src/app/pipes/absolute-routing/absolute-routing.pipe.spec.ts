import { AbsoluteRoutingPipe } from './absolute-routing.pipe';

describe('AbsoluteRoutingPipe', () => {
  it('create an instance', () => {
    const pipe = new AbsoluteRoutingPipe();
    expect(pipe).toBeTruthy();
  });
});
