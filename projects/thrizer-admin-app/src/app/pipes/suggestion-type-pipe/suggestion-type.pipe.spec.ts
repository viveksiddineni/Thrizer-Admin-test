import { SuggestionTypePipe } from './suggestion-type.pipe';

describe('SuggestionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SuggestionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
