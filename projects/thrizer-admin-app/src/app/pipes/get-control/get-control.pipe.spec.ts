import { GetControlPipe } from './get-control.pipe';

describe('GetControlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetControlPipe();
    expect(pipe).toBeTruthy();
  });
});
