import { InfiniteSelectDirective } from './infinite-select.directive';

describe('InfiniteSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiniteSelectDirective();
    expect(directive).toBeTruthy();
  });
});
