import { ForDirective } from './for.directive';

describe('ForDirective', () => {
  it('should create an instance', () => {
    const directive = new ForDirective(null);
    expect(directive).toBeTruthy();
  });
});
